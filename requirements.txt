# Find Evil! — Python dependencies
# Install: pip install -r requirements.txt
# Python 3.11+ required.

# MCP server framework (typed forensic tool surface)
mcp>=1.2.0

# Input validation for tool arguments (path/injection guardrails)
pydantic>=2.6.0

# NOTE: forensic binaries (log2timeline.py, psort.py, vol.py, rip.pl, yara,
# ewfmount, hashdeep) are provided by the SANS SIFT Workstation, not pip.
# See install.sh for environment verification.
